{
  "name": "the-gauntlet-webmcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-traps-page.mjs && node scripts/build-connect-page.mjs && node scripts/build-openapi.mjs && node scripts/build-scorecard-shell.mjs",
    "dev": "wrangler pages dev public --d1=GAUNTLET_DB",
    "deploy": "wrangler pages deploy public --project-name the-gauntlet",
    "check": "node --check public/app.js",
    "test": "node --test test/"
  },
  "devDependencies": {
    "axe-core": "^4.13.0",
    "jsdom": "^26.1.0",
    "wrangler": "^4.52.0"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
